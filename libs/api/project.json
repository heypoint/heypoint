{
  "$schema":     "../../node_modules/nx/schemas/project-schema.json",
  "name":        "api",
  "projectType": "library",
  "sourceRoot":  "libs/api/src",
  "tags":        [],
  "targets":     {
    "lint":   {
      "executor": "@nx/linter:eslint",
      "options":  {
        "lintFilePatterns": [
          "libs/api/**/*.ts"
        ]
      },
      "outputs":  [
        "{options.outputFile}"
      ]
    },
    "build":  {
      "executor": "@nx/js:tsc",
      "options":  {
        "assets":     [],
        "main":       "libs/api/main.ts",
        "outputPath": "dist/libs/api",
        "tsConfig":   "libs/api/tsconfig.lib.json"
      },
      "outputs":  [
        "{options.outputPath}"
      ]
    },
    "deploy": {
      "dependsOn": [
        "build"
      ],
      "executor":  "nx:run-commands",
      "options":   {
        "command": "firebase deploy --only functions:api"
      }
    }
  }
}
